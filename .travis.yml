language: python
python:
  - 3.8

install:
  - pip install -r requirements.txt

script:
  - pytest
  - pytest --pylint
  - coverage run -m unittest discover
  - coverage report

after_success:
  - coveralls
